{
  "buildCommand": "npm run build",
  "outputDirectory": "dist/progetto-calcio-frontend/browser",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "https://{{ env.BACKEND_URL }}/api/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
